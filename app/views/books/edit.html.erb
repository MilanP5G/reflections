<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/profile.css">
  </head>
  <body>
    <div class="header">
      <div class="banner-logo"><img src="/assets/Reflections_Logo.png"></div>
      <div class="header-right">
        <%= link_to "HOME", users_path %>
        <%= link_to "ADD BOOK", new_book_path %>
        <%= link_to "PROFILE", user_path(current_user.id) %>
        <%= link_to "ACCOUNT SETTINGS", account_settings_path(current_user.id) %>
      </div>
    </div>
    <div class="home-div">

      <div class="ayb">EDIT YOUR BOOK</div>

      <div class="nb-errors">
      <% if @book.errors.any? %>
        <% @book.errors.messages.values.each do | msg | %>
           <% msg.each do | m | %>
             <%= m %>
           <br>
           <% end %>
        <% end %>
      <% end %>
      </div>


      <%= form_for @book do | f | %>

       <div class="book-input">
        <%= f.text_field :title %>

        <%= f.text_field :author %>
        </div>
        <br>
        </br>
        <div class="upload-button"><%= f.file_field :image %></div>
        <br>
        <br>
        </br>

       <button class="create-book-button"><%= f.submit "Update Book" %></button>

      <% end %>

        </div>

      </body>
    </html>
