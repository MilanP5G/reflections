<%= form_for @book do | f | %>

  <%= f.label :title, "Book Title:" %>
  <%= f.text_field :title %>

  <%= f.label :author, "Book Author:" %>
  <%= f.text_field :author %>

  <%= f.label :image, "Upload Book Cover:" %>
  <%= f.file_field :image %>

  <%= f.submit "Create Book" %>

<% end %>
