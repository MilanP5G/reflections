<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link href="https://fonts.googleapis.com/css?family=Lato:400,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/profile.css">
  </head>
  <body>
    <div class="header">
      <div class="banner-logo"><img src="/assets/Reflections_Logo.png"></div>
      <div class="header-right">
        <%= link_to "HOME", users_path %>
        <%= link_to "ADD BOOK", new_book_path %>
        <%= link_to "PROFILE", user_path(current_user.id) %>
        <%= link_to "ACCOUNT SETTINGS", account_settings_path(current_user.id) %>
      </div>
    </div>
    <div class="home-div">



      <%= link_to @book.title, book_path(@book) %>
      <br>
      <%= @book.author %>
      <br>
      <img src="<%=(url_for(@book.image))%>">
      <br>
      </br>
      <%= @reflection.title %>
      <br>
      <br>
      <%= @reflection.content %>
      <br>
      <% if @book.owner_user == current_user %>
        <button class="erbutton"><%= link_to "Edit Reflection", edit_reflection_path(@reflection) %></button>
        <button type="drbutton"><%= link_to "Delete Reflection", reflection_path(@reflection), method: :delete %></button>
      <% end %>


        </div>

      </body>
    </html>
